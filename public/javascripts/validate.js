/***
 * 字符串是否为空
 * @param value
 * @returns {boolean}
 */
var stringIsEmpty = function (value) {
    if (value == undefined || value == "") {
        return true;
    } else {
        return false;
    }
}